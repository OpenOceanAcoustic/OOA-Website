export function PeMethodNote() {
  return (
    <div className="method-note">
      <span>
        {"REFERENCE POLICY"}
      </span>
      <p>
        {"网页固定使用 nPade=10 作为同网格高阶对照，只用于观察 Padé 截断趋势，不把它宣称为解析真值。dr、dz、环境、地形和输出采样在对比运行中保持一致；工程判定仍应使用经过数值平价验证的 OOB PE WASM 内核。"}
      </p>
    </div>
  );
}
