import { EmptyState, PlotPanel } from "@ooa/ui";
import { HeatmapCanvas } from "@ooa/visualization";
import { usePeStore } from "../state/store";
export function FieldComparisonPanel() { const state = usePeStore(); if (state.currentResult === null || state.referenceResult === null || state.difference === null) return <PlotPanel title="当前 / 参考 / 差值场"><EmptyState>运行 RAM 后显示三组保持原 TypedArray 的场。</EmptyState></PlotPanel>; const columns = state.currentResult.receiverRangesM.length; const rows = state.currentResult.receiverDepthsM.length; return <div style={{ display: "grid", gap: "1rem" }}><PlotPanel title={`当前场 · nPade=${state.parameters.nPade}`}><HeatmapCanvas values={state.currentResult.transmissionLossDb} columns={columns} rows={rows} minimum={20} maximum={120} /></PlotPanel><PlotPanel title="参考场 · nPade=10"><HeatmapCanvas values={state.referenceResult.transmissionLossDb} columns={columns} rows={rows} minimum={20} maximum={120} /></PlotPanel><PlotPanel title={`差值场 · RMS ${state.difference.rms.toFixed(3)} dB`}><HeatmapCanvas values={state.difference.values} columns={columns} rows={rows} minimum={-10} maximum={10} /></PlotPanel></div>; }
